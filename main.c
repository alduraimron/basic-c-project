#include <stdio.h>

int main() {
  //thisi is a comment:warn(":warn("");");
  /*
   *this is a comment
   * */
  int age = 19;

  float gpa = 4.0;

  printf("herllo %d with size %d\n", age, sizeof(age));
  printf("my gpa is %.1f\n", gpa);
  return 0;
}
