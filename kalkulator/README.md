# Kalkulator

This kalkulator live in terminal. it only can handle basic operation with 2 number.

## Get started

you need gcc to compile this program:
```bash
gcc main.c math.c -o kalkulator

```
and then run it:

```bash
./kalkulator

```